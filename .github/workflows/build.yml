name: Build and Deploy
on:
  push:
    branches:
      - main
  pull_request: { }

jobs:
  build:
    name: Build
    concurrency: ci-${{ github.ref }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install
        run: |
          npm ci

      - name: "Build"
        run: |
          npm run build:production

      - name: "Upload artifacts (green)"
        uses: actions/upload-artifact@v4
        with:
          name: green
          path: dist/green

      - name: "Upload artifacts (red)"
        uses: actions/upload-artifact@v4
        with:
          name: red
          path: dist/red

  deploy-green:
    name: "Deploy: green"
    concurrency: ci-${{ github.ref }}
    runs-on: ubuntu-latest
    needs: build
    if: github.ref != 'refs/heads/main'
    environment:
      name: green
      url: https://demo.engity.green
    steps:
      - name: "Download artifacts (green)"
        uses: actions/download-artifact@v4
        with:
          name: green
          path: dist

      - name: Deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          command: pages deploy --project-name=demo-engity-green dist

  deploy-red:
    name: "Deploy: red"
    concurrency: ci-${{ github.ref }}
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'
    environment:
      name: red
      url: https://demo.engity.app
    steps:
      - name: "Download artifacts (red)"
        uses: actions/download-artifact@v4
        with:
          name: red
          path: dist

      - name: Deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          command: pages deploy --project-name=demo-engity-red dist
